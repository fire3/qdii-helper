# QDII 基金数据接口文档

本文档记录 **QDII 基金限购额度** 相关公开数据接口的逆向调研结果。
所有接口均在 `2026-09-14` 实测通过，并标注了实测样本。

> ⚠️ 这些接口都是 **非官方公开 API**（天天基金 / 东方财富 的前端自用接口），
> 没有 SLA、没有版本兼容承诺，字段和路径可能随时变更。请低频调用，勿用于商业分发。

---

## 0. 快速选型

| 需求 | 推荐接口 | 单次成本 | 备注 |
|---|---|---|---|
| 全市场 QDII 限购一览 | **接口 A** 申购状态全量 | 1 次请求 / ~4 MB | 一次拿全 27538 只基金 |
| 单只基金最新额度 | **接口 B** 移动端实时信息 | 1 次请求 / 小 | 支持 CORS，可纯前端直连 |
| 单只基金静态规则 | **接口 C** F10 交易状态 | 1 次请求 / 46 KB HTML | 需解析 HTML |
| 限购变动原因 / 公告日期 | **接口 D** 基金公告 | 1 次请求 / 小 | `type=5` 是限购类公告 |
| 代码 → 名称 / 类型 | **接口 E** 全量基金列表 | 1 次请求 / ~3 MB | 可本地缓存 |
| 场内 ETF/LOF 溢价率 | **接口 F** 行情推送 | 1 次请求 / 小 | 折价率 `f402` |

---

## 1. 术语与状态枚举

### 1.1 申购状态（`申购状态` / `SGZT`）

实测取值集合（全市场去重）：

| 取值 | 含义 | 对限购查询的意义 |
|---|---|---|
| `开放申购` | 正常开放，无限额 | 无限购 |
| `限大额` | 限大额申购 | **核心关注**，需看日累计限定金额 |
| `暂停申购` | 暂停申购 | 当前不可买入 |
| `场内交易` | 只能二级市场买卖（ETF/LOF） | 不受申赎额度限制，但受溢价影响 |
| `封闭期` | 封闭运作 | 不可申购 |
| `认购期` | 正在募集 | 不可申购 |
| `""`（空） | 数据缺失 | 需回退到接口 B/C |

赎回状态（`赎回状态` / `SHZT`）是**另一套枚举**，取值与申购不同（`开放赎回` 而非 `开放申购`），
不要复用同一个枚举类型：

| 取值 | 含义 |
|---|---|
| `开放赎回` | 可赎回 |
| `暂停赎回` | 暂停赎回 |
| `场内交易` | 场内买卖 |
| `封闭期` / `认购期` | 不可赎回 |
| `""`（空） | 数据缺失 |

### 1.2 「无限额」哨兵值

**这是最关键的一个坑。** `日累计限定金额` 用一个大整数表示「无限额」，而不是空值：

| 观测值 | 含义 |
|---|---|
| `100000000000` (1e11) | 无限额 |
| `10000000000` (1e10) | 无限额（债券类常见） |
| `0` | 视状态而定：`限大额` 时为「0 元/日」；美元份额常出现 |
| 其余数值 | 真实日累计限额（单位：元） |

**判断逻辑**：`status == "限大额"` 时，若 `limit >= 1e10` 视为异常值（一般不会出现），
`status == "开放申购"` 时 `limit` 通常为 1e11，应归一化为「无限额」而非具体数字。

### 1.3 QDII 口径（重要）

`基金类型` 字段里，QDII 基金分散在 **两类标签** 中，只匹配 `QDII-` 前缀会漏掉一大半：

| 标签 | 实测数量 |
|---|---|
| `QDII-混合偏股` | 127 |
| `QDII-普通股票` | 87 |
| `QDII-纯债` | 60 |
| `QDII-混合灵活` | 29 |
| `QDII-商品` | 23 |
| `QDII-混合债` | 22 |
| `QDII-FOF` | 9 |
| `QDII-混合平衡` | 8 |
| `QDII-REITs` | 5 |
| **`指数型-海外股票`** | **365** ← 指数型 QDII 全部在这里 |

```
QDII 全集 = 基金类型.startswith("QDII-") OR 基金类型 == "指数型-海外股票"
          = 370 + 365 = 735 只（实测无重叠）
```

`270042 广发纳斯达克100ETF联接人民币(QDII)A` 这种典型 QDII 的 `基金类型` 是
`指数型-海外股票`，**不含 "QDII" 字样**。

### 1.4 份额币种

同一只基金常有多个份额类别，限额币种不同：

- `…人民币` / 无后缀 → 限额单位为 **元**
- `…美元现汇` / `…美元现钞` / `(QDII)美元A` → 限额单位为 **美元**
- 美元份额在天天基金渠道通常不销售，常出现 `日累计限定金额 = 0`

做汇总统计时应剔除 `美元` / `现汇` / `现钞` 关键字，否则会被大量 0 值拉偏。

---

## 2. 接口 A：全市场基金申购状态（主接口）

一次性拿到全市场所有基金的申购/赎回状态与限购额度，是限购查询工具的**首选数据源**。

```
GET https://fund.eastmoney.com/Data/Fund_JJJZ_Data.aspx
```

### 2.1 请求参数

| 参数 | 值 | 说明 |
|---|---|---|
| `t` | `8` | **固定为 8**，表示申购状态数据集 |
| `page` | `1,50000` | `起始页,每页条数`；给足够大即为全量 |
| `js` | `reData` | 返回 `var reData=...` 形式的 JS 变量 |
| `sort` | `fcode,asc` | 排序字段与方向 |

### 2.2 请求头

```
User-Agent: Mozilla/5.0
Referer: https://fund.eastmoney.com/Fund_sgzt_bzdm.html
```

### 2.3 实测响应

- HTTP `200`，`Content-Type: text/html; charset=utf-8`
- 响应体约 **4.1 MB**，`record` 值为 **27538** 行
- 响应体是 **JS 对象字面量，不是合法 JSON**（部分 key 无引号）

```
var reData={datas:[["000001","华夏成长混合","混合型-灵活","1.2350","09-14","开放申购","开放赎回","","10.0","100000000000","1.0","1","0.15%"], ... ],record:"27538",pages:"1",curpage:"1",showday:["2026-09-14","2026-09-11"]}
```

### 2.4 解析要点

`datas` 内层行是数组，可以直接 JSON 解析；**只有外层 key 无引号**，因此：

```python
import json, re

t = resp.text.strip()
arr = t[t.index("datas:[") + len("datas:"): t.index("],record:") + 1]
rows = json.loads(arr)                      # 27538 行，每行 13 列
meta = dict(re.findall(r'(\w+):("[^"]*"|\[[^\]]*\])', t[t.index("record:"):]))
# meta = {'record': '"27538"', 'pages': '"1"', 'curpage': '"1"',
#         'showday': '["2026-09-14","2026-09-11"]'}
```

也可以在开头做一次 `re.sub(r"^\{datas:", '{"datas":', t)`，但**尾部还缺 4 个 key**，
截取数组是最稳的做法。

### 2.5 字段字典（每行 13 列）

| # | 字段 | 类型 | 示例 | 说明 |
|---|---|---|---|---|
| 0 | 基金代码 | string | `270042` | 6 位 |
| 1 | 基金简称 | string | `广发纳斯达克100ETF联接人民币(QDII)A` | 含份额后缀 |
| 2 | 基金类型 | string | `指数型-海外股票` | 见 §1.3 |
| 3 | 最新净值/万份收益 | string→float | `8.1177` | 货币基金为万份收益 |
| 4 | 最新净值报告时间 | string | `09-11` | **仅 MM-DD，无年份**。QDII 常滞后 T-1/T-2 |
| 5 | 申购状态 | string | `限大额` | 见 §1.1 |
| 6 | 赎回状态 | string | `开放赎回` | 见 §1.1 |
| 7 | 下一开放日 | string | `""` 或 `2026-12-08` | 定期开放基金才有值 |
| 8 | 购买起点 | string→float | `2.0` | 单位：元 |
| 9 | **日累计限定金额** | string→float | `2.0` | **单位：元**，见 §1.2 |
| 10 | 未知 | string | `1.0` | 疑似风险等级/份额类型，未证实 |
| 11 | 未知 | string | `1` | 未证实 |
| 12 | 手续费 | string | `0.13%` | 天天基金折扣费率，`""` 表示无 |

### 2.6 实测样本行

```
270042  广发纳斯达克100ETF联接人民币(QDII)A  指数型-海外股票  8.1177 09-11  限大额   开放赎回  ""  2.0  2.0    1.0 1 0.13%
000834  大成纳斯达克100ETF联接(QDII)A        指数型-海外股票  6.2705 09-11  限大额   开放赎回  ""  10.0 10.0   1.0 1 0.12%
006479  广发纳斯达克100ETF联接人民币(QDII)C   指数型-海外股票  7.9752 09-11  限大额   开放赎回  ""  2.0  2.0    0.0 1 0.00%
501312  华宝海外科技股票(QDII-LOF)A          QDII-普通股票    2.3686 09-11  限大额   开放赎回  ""  10.0 20.0   1.0 1 0.12%
050025  博时标普500ETF联接A                 指数型-海外股票  5.5759 09-11  暂停申购 开放赎回  ""  10.0 100.0  1.0 4 0.12%
513100  纳指ETF国泰                        指数型-海外股票  1.9831 09-11  场内交易 场内交易  ""  0    0      0   "" ""
```

> `270042` / `006479` 日限额 **2 元**、`000834` **10 元** —— 这就是 2026 年 QDII 限购的实际情况。

### 2.7 实测分布（QDII，共 735 只）

人民币份额（250 只，剔除美元/现汇/现钞）：

| 申购状态 | 数量 |
|---|---|
| 限大额 | 127 |
| 开放申购 | 93 |
| 暂停申购 | 28 |
| 封闭期 / 空 | 2 |

`限大额` 的日限额档位分布（出现次数）：

```
100000 (10万)  20 只      1000 (1千)   16 只
10000  (1万)   14 只      100      (百) 13 只
1000000(百万)   9 只      0             8 只
2000           8 只       50000         7 只
```

---

## 3. 接口 B：单基金实时信息（移动端）

单只基金最全的实时字段，**且支持 CORS**，适合前端直连。

```
GET https://fundmobapi.eastmoney.com/FundMNewApi/FundMNBasicInformation
```

### 3.1 请求参数

| 参数 | 值 | 说明 |
|---|---|---|
| `FCODE` | `270042` | 基金代码 |
| `deviceid` | `1` | 任意字符串 |
| `plat` | `Android` | 平台标识 |
| `product` | `EFund` | 产品标识 |
| `version` | `6.2.8` | 版本号，随意 |

### 3.2 跨域（实测）

```
HTTP/1.1 200 OK
Access-Control-Allow-Origin: *
Access-Control-Allow-Headers: Content-Type
Access-Control-Allow-Methods: GET,POST,PUT,DELETE,OPTIONS
```

→ 纯前端（静态页 + fetch）即可直连，无需自建后端。这是同类开源项目
`zddhhh/qdii-monitor` 采用的技术路线。

### 3.3 限购相关字段

响应结构：`{ "Datas": { ... }, "ErrCode": ..., "Success": true }`

| 字段 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `FCODE` | string | `270042` | 基金代码 |
| `SHORTNAME` | string | `广发纳斯达克100ETF联接人民币(QDII)A` | 简称 |
| `FTYPE` | string | `指数型-海外股票` | 类型 |
| `SGZT` | string | `限大额` | **申购状态**，枚举同 §1.1 |
| `SHZT` | string | `开放赎回` | 赎回状态 |
| `MINSG` | string | `2` | **最小申购金额（元）** |
| `MAXSG` | string | `2` | **单日最大申购金额**，`100000000000` = 无限额 |
| `MINSBSG` | string | `2` | 最小申购（本币） |
| `MINSBRG` | string | `100` | 最小认购（本币） |
| `MINRG` | string | `100` | 最小认购 |
| `MINDT` | string | `10` | 定投起点 |
| `DWJZ` | string | `8.1177` | 单位净值 |
| `FSRQ` | string | `2026-09-11` | 净值日期（**含年份**，优于接口 A） |
| `DUEDATE` | string | `"--"` | 下一开放日，无则为 `"--"` |
| `SOURCERATE` | string | `1.30%` | 原费率 |
| `RATE` | string | `0.13%` | 折后费率 |
| `JJGS` | string | `银华基金` | 基金公司 |
| `JJJL` | string | `王晓川` | 基金经理 |
| `RISKLEVEL` | string | `4` | 风险等级 |
| `BUY` | bool | `true` | 是否可购买 |
| `ISSALES` | string | `1` | 是否在售 |
| `YFD…` | — | — | 其它非限购字段 |

### 3.4 实测样本

```
270042  限大额    MINSG=2   MAXSG=2      DWJZ=8.1177   FSRQ=2026-09-11
000834  限大额    MINSG=10  MAXSG=10     DWJZ=6.2705   FSRQ=2026-09-11
050025  暂停申购  MINSG=10  MAXSG=100    DWJZ=5.5759   FSRQ=2026-09-11
513100  场内交易  MINSG=--  MAXSG=--     DWJZ=1.9831   FSRQ=2026-09-11
015641  开放申购  MINSG=10  MAXSG=100000000000 (无限额)
```

### 3.5 注意事项

- `MAXSG` 与接口 A 的 `日累计限定金额` 语义一致，但**格式不同**（这里是纯数字字符串）
- `场内交易` 类基金 `MINSG/MAXSG` 返回 `"--"`，需容错
- 无 `Referer` 校验，可直接请求

---

## 4. 接口 C：F10 交易状态页

单只基金的**完整交易规则**，含持仓上限等接口 A/B 没有的字段。

```
GET https://fundf10.eastmoney.com/jjfl_270042.html
```

### 4.1 实测响应

HTTP `200`，约 46 KB HTML。关键单元格（`<td>` 平铺，需配对解析）：

| 标签 | 实测值（015641） |
|---|---|
| 申购状态 | `开放申购` |
| 赎回状态 | `开放赎回` |
| 定投状态 | `支持` |
| **申购起点** | `10.00元` |
| 定投起点 | `10.00元` |
| **日累计申购限额** | `无限额` |
| 首次购买 | `10.00元` |
| 追加购买 | `10.00元` |
| **持仓上限** | `无限额` |
| 最小赎回份额 | `1.00份` |
| 部分赎回最低保留份额 | `1.00份` |
| 买入确认日 | `T+1` |

### 4.2 解析要点

页面是「标签 `<td>` → 值 `<td>`」交替结构，正则配对即可：

```python
import re
pairs = re.findall(
    r'<td class="th[^"]*"[^>]*>(.*?)</td>\s*<td[^>]*>(.*?)</td>',
    html, re.S,
)
info = {re.sub(r"<[^>]+>", "", k).strip(): re.sub(r"<[^>]+>", "", v).strip()
        for k, v in pairs}
# info["日累计申购限额"] -> "无限额" 或 "1,000.00元"
```

- 限额值有两种形态：文字 `无限额` 或 `1,000.00元`，需分别处理
- 页面同时含费率表格，正则时注意不要串行匹配

### 4.3 适用场景

补全「持仓上限」等字段；或作为 A/B 接口的交叉校验。

---

## 5. 接口 D：基金公告（限购原因 / 生效日期）

QDII 限购的**权威口径**来自基金公司公告。该接口可按公告类型筛选。

```
GET https://api.fund.eastmoney.com/f10/JJGG
```

### 5.1 请求参数

| 参数 | 值 | 说明 |
|---|---|---|
| `callback` | `cb` | JSONP 回调名 |
| `fundcode` | `270042` | 基金代码 |
| `pageIndex` | `1` | 页码（从 1 开始） |
| `pageSize` | `20` | 每页条数 |
| `type` | `5` | 公告类型，见下表 |

### 5.2 `type` 枚举（实测）

| type | 内容 | 实测标题样例 |
|---|---|---|
| `1` | 全部 / 招募说明书类 | `…联接基金(QDII)更新的招募说明书` |
| `2` | 分红类 | `…基金收益分配公告` |
| `3` | 定期报告 | `…2026年中期报告` / `…2026年第2季度报告` |
| `4` | 人事调整 | `…基金经理变更公告` |
| **`5`** | **申购赎回类（限购公告）** | `…人民币份额调整大额申购…业务限额的公告` |
| `6` | 公司级通知 | `…旗下基金2026年中期报告提示性公告` |
| `7` | 空 | — |

> **`type=5` 是限购查询的入口**：暂停/限制大额申购的公告全部在此。

### 5.3 实测响应

```json
cb({
  "Data": [
    {
      "FUNDCODE": "270042",
      "TITLE": "广发基金管理有限公司关于广发纳斯达克100交易型开放式指数证券投资基金联接基金(QDII)人民币份额调整大额申购(含转换转入、定期定额和不定额投资)业务限额的公告",
      "ShortTitle": "广发纳指100ETF联接人民币(QDII)A",
      "NEWCATEGORY": "5",
      "PUBLISHDATE": "2026-09-10T00:00:00",
      "PUBLISHDATEDesc": "2026-09-10",
      "ATTACHTYPE": "0",
      "ID": "AN202609101828123456"
    }
  ],
  "TotalCount": 353,
  "ErrCode": 0,
  "Success": true
})
```

### 5.4 关键字段

| 字段 | 说明 |
|---|---|
| `TITLE` | 公告全称，含「暂停申购 / 调整大额申购限额」等关键动作 |
| `PUBLISHDATEDesc` | 公告日期 `YYYY-MM-DD` |
| `NEWCATEGORY` | 类型码，与请求 `type` 对应 |
| `TotalCount` | 该基金该类型公告总数 |
| `ID` | 公告 ID，详情页：`https://fund.eastmoney.com/gonggao/{FUNDCODE},{ID}.html` |

### 5.5 请求头

```
User-Agent: Mozilla/5.0
Referer: https://fundf10.eastmoney.com/
```

---

## 6. 接口 E：全量基金列表

```
GET https://fund.eastmoney.com/js/fundcode_search.js
```

实测约 3.1 MB，UTF-8（带 BOM），返回：

```js
var r = [["000001","HXCZHH","华夏成长混合","混合型-灵活","HUAXIACHENGZHANGHUNHE"], ...]
```

| # | 字段 |
|---|---|
| 0 | 基金代码 |
| 1 | 拼音缩写 |
| 2 | 基金简称 |
| 3 | 基金类型（**口径同 §1.3**） |
| 4 | 拼音全称 |

用于：搜索联想（代码/名称/拼音）、`基金代码 → 类型` 映射，建议本地缓存。

---

## 7. 接口 F：场内行情（含折价率/溢价率）

QDII ETF/LOF 无法场外申购时，溢价率是决策关键。实测 QDII ETF 普遍**大幅溢价**。

### 7.1 批量列表

```
GET https://push2delay.eastmoney.com/api/qt/clist/get
```

| 参数 | 值 |
|---|---|
| `pn` | 页码 |
| `pz` | 每页条数 |
| `po` / `np` | `1` / `1` |
| `fltt` | `2`（**输出十进制小数，不加则返回放大整数**） |
| `invt` | `2` |
| `fid` | 排序字段，如 `f3` |
| `fs` | 板块，`b:MK0021,b:MK0022,b:MK0023,b:MK0024`（沪深 ETF+LOF） |
| `fields` | `f12,f14,f2,f3,f402` |

实测 `total = 1604`。

### 7.2 单只 / 批量指定代码

```
GET https://push2delay.eastmoney.com/api/qt/ulist.np/get?fltt=2&fields=f2,f12,f14,f18,f402&secids=1.513500,0.159941
```

`secids` 格式：`{市场}.{代码}`，`1` = 上交所，`0` = 深交所。

### 7.3 字段字典

| 字段 | 含义 | 实测（513500） |
|---|---|---|
| `f2` | 最新价 | `2.656` |
| `f3` | 涨跌幅 % | `-0.19` |
| `f4` | 涨跌额 | `-0.005` |
| `f12` | 代码 | `513500` |
| `f13` | 市场（1=沪, 0=深） | `1` |
| `f14` | 名称 | `标普500ETF博时` |
| `f18` | 昨收 | `2.661` |
| **`f402`** | **折价率 %（负值 = 溢价）** | `-8.24` |
| `f403` | 区间涨跌幅 % | `21.44` |

### 7.4 `f402` 语义验证

用「T-1 单位净值」作为基准复算：

| 代码 | 价格 `f2` | 净值 `DWJZ` | 溢价 = (价格-净值)/净值 | `f402` |
|---|---|---|---|---|
| 513500 | 2.656 | 2.4542 | **+8.22%** | **-8.24** |
| 513100 | 2.191 | 1.9831 | **+10.48%** | **-10.47** |
| 159941 | 1.624 | 1.4848 | **+9.37%** | **-9.36** |

→ 结论：`f402 = (净值 - 价格) / 净值 × 100 = -溢价率`。
`f402` 为负说明**场内价格高于净值（溢价）**。QDII ETF 溢价 8%~10% 是常态。

实测最高溢价（`2026-09-14`）：

| 代码 | 名称 | 价格 `f2` | 净值 `DWJZ` | 复算溢价 | `f402` |
|---|---|---|---|---|---|
| 159509 | 纳指科技ETF景顺 | 2.799 | 2.2751 | **+23.03%** | **-22.97** |
| 159501 | 纳指ETF嘉实 | 2.083 | 1.8627 | **+11.83%** | **-11.82** |
| 159513 | 纳斯达克100ETF大成 | 1.766 | 1.6560 | **+6.64%** | **-6.60** |

> 溢价 **23%** 意味着：场外限购 10 元时转战场内，等于多付两成成本。
> 这是 QDII 限购查询必须同时给出溢价率的原因。

### 7.5 域名可用性

| 域名 | 实测 |
|---|---|
| `push2.eastmoney.com` | 主域名，偶尔连接超时 |
| `push2delay.eastmoney.com` | 备用（延时行情），实测稳定 |

建议**主备自动切换**。

---

## 8. 已失效 / 弃用接口

| 接口 | 状态 | 说明 |
|---|---|---|
| `https://fundgz.1234567.com.cn/js/{code}.js` | **已失效** | 旧「盘中估值」接口，实测任意代码（`000001`/`270042`/`161725`）均返回 **404 页面**。仍有开源项目引用，勿再使用。 |
| `http://fund.eastmoney.com/Fund_sgzt_bzdm.html` | 页面可用 | 申购状态**页面**本身，数据由接口 A 填充；直接抓页面无意义 |

盘中估值替代方案：场内用接口 F（`f2` 最新价）；场外用接口 B 的 `DWJZ` + `FSRQ`。

---

## 9. 参考实现

| 项目 | 语言 | 价值 |
|---|---|---|
| [akfamily/akshare](https://github.com/akfamily/akshare) | Python | `fund_purchase_em()` 即接口 A 的封装（见 `akshare/fund/fund_em.py`）。已验证其实现使用 `Fund_JJJZ_Data.aspx?t=8`。字段命名存在 `"序号"` 占位列与两个 `"-"` 未命名字段。 |
| [kouchao/TiantianFundApi](https://github.com/kouchao/TiantianFundApi) | Node.js | 天天基金接口的成体系整理 |
| [zddhhh/qdii-monitor](https://github.com/zddhhh/qdii-monitor) | 纯前端 | 直接使用接口 B + CORS 做静态页；数据存 `data.json`，GitHub Actions 定时更新。QDII 口径「700+ 只」与本文件 §1.3 的 735 只一致。 |

---

## 10. 综合注意事项

1. **合规**：以上均为非官方接口，请控制频率（建议 ≥ 30 分钟一次），禁止高频轮询。
2. **免责**：限购额度最终以基金公司公告为准，工具输出仅供参考。
3. **时效**：
   - 接口 A 的 `showday` 显示数据日期，QDII 净值天然滞后（实测 `09-11` vs 当日 `09-14`）
   - 接口 A 的净值日期只有 `MM-DD`，跨年需自行补年份；接口 B 的 `FSRQ` 有完整年份
4. **编码**：接口 A `/ E` 均为 UTF-8；接口 E 带 BOM，读取时用 `utf-8-sig`
5. **容错**：所有状态字段都可能为空字符串；`MINSG/MAXSG` 可能为 `"--"`；`日累计限定金额` 可能是 `0`
6. **可达性**：`fund.eastmoney.com` / `fundf10.eastmoney.com` / `api.fund.eastmoney.com` /
   `fundmobapi.eastmoney.com` 实测均可在无代理环境直接访问；`push2` 主备切换见 §7.5
